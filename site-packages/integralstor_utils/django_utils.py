
def in_request_parameters(request, param_name):
    result = False
    try:
        if not request:
            raise Exception('Request object not provided')
        if not param_name:
            raise Exception('Parameter name not provided')
        if request.method == 'GET':
            if param_name in request.GET:
                result = True
        elif request.method == 'POST':
            if param_name in request.POST:
                result = True
        else:
            raise Exception('Unsupported request method : %s' % request.method)
    except Exception, e:
        return None, 'Error checking request parameters : %s' % str(e)
    else:
        return result, None


def get_request_parameter_value(request, param_name):
    val = None
    try:
        if not request:
            raise Exception('Request object not provided')
        if not param_name:
            raise Exception('Parameter name not provided')
        if request.method == 'GET':
            if param_name in request.GET:
                val = request.GET.get(param_name)
        elif request.method == 'POST':
            if param_name in request.POST:
                val = request.POST.get(param_name)
        else:
            raise Exception('Unsupported request method : %s' % request.method)
    except Exception, e:
        return None, 'Error checking request parameters : %s' % str(e)
    else:
        return val, None

# vim: tabstop=8 softtabstop=0 expandtab ai shiftwidth=4 smarttab
